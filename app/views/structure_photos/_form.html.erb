<%= form_for(structure_photo) do |f| %>
  <% if structure_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(structure_photo.errors.count, "error") %> prohibited this structure_photo from being saved:</h2>

      <ul>
      <% structure_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %>
    <%= f.text_field :uuid %>
  </div>

  <div class="field">
    <%= f.label :base64image %>
    <%= f.text_area :base64image %>
  </div>

  <div class="field">
    <%= f.label :filename %>
    <%= f.text_field :filename %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
